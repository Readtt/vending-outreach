# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# typescript
*.tsbuildinfo
next-env.d.ts

# dry-run outbox — .eml files written instead of sending
/outbox-dryrun/

# engine kill switch (spec §1)
/STOP

# subagent-driven-development scratch
/.superpowers/

# local agent/editor state
/.claude/
/.cursor/

# exported conversation transcripts
/*-local-command-caveat*.txt
/20??-??-??-*.txt

# scratch / saved pages
/yt1.html
/*.local.md

# Runtime data — the SQLite database and anything else the app writes.
# Kept inside the project on purpose (see lib/paths.ts) and never committed.
/data/
*.db
*.db-wal
*.db-shm
